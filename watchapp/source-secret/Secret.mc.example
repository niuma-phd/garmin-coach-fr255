// TEMPLATE ONLY. The real source-secret/Secret.mc is generated at build time by
// build-app.sh (from $COACH_INGEST_TOKEN or ~/减肥机制/config/secrets.toml [ingest].token)
// and is gitignored — the Bearer token must never enter the public repo.
(:background)
module Secret {
    const COACH_URL = "https://api.jungerpf.top/coach/ingest";
    const COACH_TOKEN = "";   // empty here → app compiles but skips upload until provisioned
}
